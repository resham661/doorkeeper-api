<h3><%= "Hello, #{current_user.email}" %></h3>
<% if user_signed_in? %>
  <%= button_to "Sign_out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<% else %>
  <%= link_to "Sign in", new_user_session_path %>
  <%= link_to "Sign up", new_user_registration_path %>
<% end %>

<p>You can test the API with following credentials.</p>
<div class="container">
  <ul>
    <li><%= link_to "Web Books", books_path %></li>
    <li>API V1 Books</li>
    <li>API V1 Android Books</li>
  </ul>
</div>
